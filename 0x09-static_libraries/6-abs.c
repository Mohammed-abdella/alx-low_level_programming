#include "main.h"

/**
 * _abs - to print the absolute value of an input number
 * @c: the number whose absolute value is to be returned
 *
 * Return: the absolute value of the input number
 */
int _abs(int c)
{
	if (c >= 0)
		return (c);
	else
		return (-c);
}
